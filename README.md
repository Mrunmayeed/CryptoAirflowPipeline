## Bitcoin Airflow Pipeline

In this project, We build a machine learning pipeline which reads dataset from Minio S3. It then preprocesses and scales this dataset. An ARIMA model is trained on this dataset. The trained scaler and model is then stored back in the S3
